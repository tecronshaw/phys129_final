import numpy as np


def run(image_data):
    """
    Performs the k-means clustering algorithm which produces an image where the original pixel values
    are dynamically binned by relative close-ness in color space
    :return:
    """
    print("Running cluster")
    return image_data
